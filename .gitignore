__pycache__
.vscode
uv.lock
*backup
experiments


# Environment files
.env
.env.sentinel
.env.api
.env.shared
.env.local
.env.*.local

# Keep examples
!.env.sentinel.example
!.env.api.example
!.env.shared.example

# =====================================================
# AWS & Deployment
# =====================================================

# AWS credentials
.aws/
*.pem
*.key

# Deployment artifacts
dist/
build/

# Lambda deployment packages
*.zip
lambda/*.zip
terraform/lambda/*.zip


# =====================================================
# Terraform .gitignore
# =====================================================

# Local .terraform directories
**/.terraform/*

# .tfstate files (NEVER commit state files!)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (may contain sensitive data)
*.tfvars
*.tfvars.json

# Override files (local development overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Terraform lock file for providers (SHOULD be committed, but excluding here if you want)
# Remove the line below if you want to commit .terraform.lock.hcl
# .terraform.lock.hcl

# Ignore Terraform plan output files
*.tfplan
*.tfplan.*
plan.out
plan.json

# Terraform state backup files
*.backup

# Terraform variable files (if they contain secrets)
# Comment out if your .tfvars don't contain secrets
secrets.tfvars
terraform.tfvars

# Terraform lock info
.terraform.tfstate.lock.info


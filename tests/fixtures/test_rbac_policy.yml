version: "1.1"
roles:
  operator:
    routes_allowed: [SOP, EVENTS, TRACKING]
    tools_allowed:
      - who_entered_zone
      - send_track
  viewer:
    routes_allowed: [SOP]
    tools_allowed:
      - search_sop